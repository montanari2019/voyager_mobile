import { Text, View } from "react-native";
import { styled } from "./styled";

export function Sobras(){
    return(
        <View style={styled.container}>
            <Text style={styled.textTitle}>Sobras</Text>
        </View>
    )
}